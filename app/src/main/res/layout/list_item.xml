<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/containerView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    tools:background="@color/redColor">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
            <TextView
                android:id="@+id/score_text_view"
                style="@style/history_view_style"
                tools:text="111"/>
            <TextView
                android:id="@+id/corner_scored_text_view"
                style="@style/history_view_style"
                android:padding="0dp"
                tools:text="Red" />

        <TextView
                android:id="@+id/points_scored_text_view"
                style="@style/history_view_style"
                tools:text="+4 points"/>
    </LinearLayout>
    <View
        android:layout_width="match_parent"
        android:layout_height="@dimen/seperation_view_thickness"
        android:background="@color/history_item_devider"/>
</LinearLayout>
